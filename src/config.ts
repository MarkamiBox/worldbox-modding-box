/**
 * Project-wide links. One place to change when the repo moves or a channel is added.
 * Everything here is public information: never put a token or a private URL in this file.
 */

/** GitHub repository that hosts this site. Feedback opens issues here. */
export const REPO_URL = 'https://github.com/MarkamiBox/worldbox-modding-box';

/** Public chat, or an empty string to hide the link. */
export const DISCORD_URL = 'https://discord.gg/worldbox';

export const ISSUES_URL = `${REPO_URL}/issues`;
export const NEW_ISSUE_URL = `${REPO_URL}/issues/new`;
